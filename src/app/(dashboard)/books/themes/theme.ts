import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#27a098", // Change this to your desired color
    },
    secondary: {
      main: "#f50057", // Change this to your desired secondary color
    },
  },
});

export default theme;
